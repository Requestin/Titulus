{
  "name": "@titulus/runtime",
  "version": "0.1.0",
  "private": true,
  "description": "Titulus shared render-logic package (JSON template -> DOM). Source of truth for the engine channel page, editor preview, and thumbnails. DEVELOPMENT_PROMPT §6.",
  "license": "UNLICENSED",
  "type": "module",
  "main": "./src/index.ts",
  "types": "./src/index.ts",
  "scripts": {
    "test": "tsx --test tests/*.test.ts",
    "typecheck": "tsc --noEmit",
    "build": "node build.mjs",
    "clean": "rm -rf dist ../backend/public/bg-runtime.js"
  },
  "devDependencies": {
    "esbuild": "^0.23.0",
    "tsx": "^4.23.1",
    "typescript": "^5.5.0"
  }
}
