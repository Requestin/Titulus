# bench/ — perf harness

Сцены нагрузки для `bg_engine`. Результаты Phase 0: `docs/development-phases/phase-00-engine-bench.md`.

```bash
./run-bench.sh 3 20 5          # быстрый прогон
./run-bench.sh 3 1800 5        # soak 30 мин
```

Сцены: `bench.html`, `bench-alpha.html`, `bench-25d.html`, `bench-mask-stack.html`, `bench-wipe-*.html`.
