import { Placeholder } from '@/components/Placeholder';

export function ControlPage() {
  return (
    <Placeholder title="Control panel">
      The operator panel (TAKE / UPDATE / CLEAR, channels, rundowns, program monitor) lands in task 2.7.
    </Placeholder>
  );
}
