import { Placeholder } from '@/components/Placeholder';

export function SettingsPage() {
  return (
    <Placeholder title="Settings">
      Channels and output modes (browser / OBS·vMix / DeckLink / stream) land in task 2.8.
    </Placeholder>
  );
}
